const idade = 70
const genero = "f"
const nome = "jurema"
const contribuição = 20

if (genero == "f" &&  (idade + contribuição) >= 85)(
        console.log("parabéns vc pode se aposentar")
    )
    else if  (genero == "m" &&  (idade + contribuição) >= 95)(
        console.log("parabéns vc pode se aposentar man")
    )
    else  (
        console.log("vc ainda n pode se aposentar")
    )
